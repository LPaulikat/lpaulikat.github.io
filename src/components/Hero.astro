<section class="section-dark" aria-labelledby="hero-title">
    <div class="hero-container">
        <div class="hero-text">
            <div class="hero-headline">
                <p class="section-label">Rettungshundeverein</p>
                <h1 id="hero-title" class="hero-title">
                    RESCUE
                    <span class="red">DOGS</span>
                    <br />
                    <span>Schleswig-Flensburg e.V.</span>
                </h1>
            </div>
            <p class="section-sub">
                Der Verein Rescue Dogs Schleswig-Flensburg e.V. widmet sich der Ausbildung und dem Einsatz von
                Rettungshundeteams zur Suche nach vermissten Personen. Grundlage bildet der Ausbildungsstandard der <strong
                    >Interessengemeinschaft Rettungshunde (IGR).</strong
                >
                Wir verfolgen ausschließlich und unmittelbar gemeinnützige Zwecke.
            </p>
            <div class="hero-cta">
                <a href="#contact" class="btn btn-primary">Kontakt aufnehmen</a>
                <a href="#about" class="btn btn-outline">Mehr erfahren</a>
            </div>
        </div>
        <img class="hero-visual" src="/images/collage.jpg" alt="Eine Collage von Bildern mit Hunden" />
    </div>
</section>

<style>
    .hero-container {
        display: grid;
        gap: 1.5rem;
        max-width: 1920px;
        margin: 0 auto;
        padding-block: 4rem;
        padding-inline: 1rem;

        @media (width> 1024px) {
            grid-template-columns: 1fr 1fr;
            padding-inline: 5rem;
            padding-block: 8rem;
        }
        .hero-headline {
            .section-label {
                color: var(--red);
            }
        }
    }

    .hero-text {
        display: grid;
        gap: 3rem;

        .section-sub {
            max-width: 47rem;
            margin: 0;
        }
    }

    .hero-title {
        .red {
            color: var(--red);
        }
    }

    .hero-cta {
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
    }

    .hero-visual {
        width: 100%;
        border-radius: 15px;
        object-fit: contain;
    }
</style>
