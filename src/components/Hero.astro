<section class="section-dark" aria-labelledby="hero-title">
    <div class="hero-container">
        <div class="hero-text">
            <p class="section-label">Rettungshundeverein</p>
            <h1 id="hero-title" class="hero-title">
                RESCUE
                <span class="red">DOGS</span>
                <br />
                <span>Schleswig-Flensburg e.V.</span>
            </h1>
            <p class="section-sub">
                Der Verein Rescue Dogs Schleswig-Flensburg e.V. widmet sich der
                Ausbildung und dem Einsatz von Rettungshundeteams zur Suche nach
                vermissten Personen. Grundlage bildet der Ausbildungsstandard
                der <strong>Interessengemeinschaft Rettungshunde (IGR).</strong>
                Wir verfolgen ausschließlich und unmittelbar gemeinnützige Zwecke.
            </p>
            <div class="hero-cta">
                <a href="#contact" class="btn btn-primary">Kontakt aufnehmen</a>
                <a href="#about" class="btn btn-outline">Mehr erfahren</a>
            </div>
            <div class="hero-stats">
                <div class="hero-stat">
                    <div class="hero-stat-num">23</div>
                    <div class="hero-stat-label">Aktive Mitglieder</div>
                </div>
                <div class="hero-stat">
                    <div class="hero-stat-num">14</div>
                    <div class="hero-stat-label">Ausgebildete Hunde</div>
                </div>
                <div class="hero-stat">
                    <div class="hero-stat-num">12</div>
                    <div class="hero-stat-label">Jahre aktiv</div>
                </div>
            </div>
        </div>
        <img
            class="hero-visual"
            src="/images/collage.jpg"
            alt="Eine Collage von Bildern mit Hunden"
        />
    </div>
</section>

<style>
    .hero-container {
        display: grid;
        gap: 1.5rem;
        max-width: 1920px;
        margin: 0 auto;
        padding-block: 4rem;
        padding-inline: 1rem;

        @media (width> 1024px) {
            grid-template-columns: 1fr 1fr;
            padding-inline: 5rem;
            padding-block: 8rem;
        }
        .section-label {
            color: var(--red);
            margin-bottom: -1rem;
        }
    }

    .hero-text {
        display: grid;
        gap: 2rem;

        .section-sub {
            max-width: 47rem;
        }
    }

    .hero-title {
        .red {
            color: var(--red);
        }
    }

    .hero-cta {
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
    }

    .hero-stats {
        display: flex;
        gap: 1.5rem;
        flex-direction: column;
        padding-top: 2rem;

        @media (width> 1024px) {
            margin-top: 3rem;
            flex-direction: row;
            border-top: 1px solid #2a2a2a;
        }
    }

    /*.hero-stat {
        @media (width> 1024px) {
             {
                border-left: 1px solid #2a2a2a;
                padding-left: 1.5rem;
            }
        }
    }*/

    .hero-stat-num {
        font-family: "Saira", sans-serif;
        font-size: 2.5rem;
        color: var(--red);
        line-height: 1;
    }

    .hero-stat-label {
        font-size: 1rem;
        color: var(--gray);
        letter-spacing: 2px;
        text-transform: uppercase;
        margin-top: 0.25rem;
    }

    .hero-visual {
        width: 100%;
        /*height: 100%;*/
        object-fit: contain;
    }
</style>
